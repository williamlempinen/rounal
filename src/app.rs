use crate::{journal, system, ui::UiView, util};

use anyhow::Result;

struct App {
    quit: bool,
    current_view: UiView,
}

impl App {
    fn new() -> Self {
        Self
    }
}

pub fn start() -> Result<()> {
    Ok(())
}

fn run() -> Result<()> {
    Ok(())
}
